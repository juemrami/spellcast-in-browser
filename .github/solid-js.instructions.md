---
---

use `<Show>` when dealing with conditional rendering (as opposed to ternary or `&&` operators)

use `<For>` when rendering lists (as opposed to `map`)

Solid-js docs can be found in .agents/repos/ directory, if they are not there , then re-clone them from https://github.com/solidjs/solid-docs
